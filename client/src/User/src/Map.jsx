import React from 'react';

class Map extends React.Component {
   render() {
      return (
      <div>
          <h2 className = "check">Map</h2>
          <h3 className = "check">This is the individual map being redirected to.</h3>
          <p className = "check"> Includes weather info and the representation of location. </p>

        </div>
      );
   }
}

export default Map;
